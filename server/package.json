{
  "name": "server",
  "version": "1.0.0",
  "description": "Backend server for hackathon website",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "echo \"No build step required\"",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongoose": "^7.0.3",
    "razorpay": "^2.8.4"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
